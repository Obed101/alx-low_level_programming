#include <stdio.h>
/**
  *main - Entry point
  *
  *Return: Always 0 (Success)
  */
int main(void)
{
        char alphabets = 'a';
	char caps = 'A';
	while (alphabets <= 'z'( caps <= 'A'))
{
        putchar(alphabets);
        alphabets++;
	putchar(caps);
	caps++;
}
putchar('\n');
return (0);
}
