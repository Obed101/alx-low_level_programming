#include "holberton.h"
/**
 *factorial - returns the factors multiplied
 *
 *@n: number/integer
 *
 *return: 0 or -1
 */
int factorial(int n)
{
	if (n < 0)
		return (-1);

	if (n = 0)
		return (n * factorial(n));
}
