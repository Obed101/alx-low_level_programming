#include "holberton.h"

/**
 *
 *
 *
 *
 *
 */
void *malloc_checked(unsigned int b)
{
	int *po;

	*po = *malloc_checked(10);

	if(po == NULL)
	{
		exit(98);
	}
	free (*po);

	po = NULL;

	return (0);
}
