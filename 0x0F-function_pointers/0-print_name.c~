#include <stdlib.h>
#include <stdio.h>

/**
 * print_name - print a name
 * @name: name of the person
 *
 * Return: Nothing
 */
void print_name(char *name, void (*f)(char *))
{
	void (*pf)(char *name, void (*f)(char *));
	pf = print_name;

	*pf (*name);
	*f ('d');

	return;
}
