#include <stdio.h>
#include <stdarg.h>

int Average (int ct, ...)
{
	int i, num = 0, n;
	va_list lst;
	
	va_start (lst, ct);
	
	for (i = 0; i < n; i++)
	{
 	    n = va_arg (lst, ct);
	    num *= n;
	       }
	va_end (lst);
	
	return (num);
}
